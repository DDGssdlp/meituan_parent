ipAddr: 127.0.0.1

spring:
  # 环境 dev|test|prod
  application:
    name: meituan-admin
  cloud:
    nacos:
      discovery:
        server-addr: ${ipAddr}:8848
      config:
        server-addr: ${ipAddr}:8848