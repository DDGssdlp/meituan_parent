ipAddr: 101.200.140.80

spring:
  # 环境 dev|test|prod
  application:
    name: meituan-admin
  cloud:
    nacos:
      discovery:
        server-addr: ${ipAddr}:8848
      config:
        server-addr: ${ipAddr}:8848